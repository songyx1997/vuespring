server:
  port: 8001

mybatis:
  mapper-locations: classpath:/mapper/*.xml

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/library?useUnicode=yes&characterEncoding=UTF-8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver